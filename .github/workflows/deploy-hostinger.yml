name: Deploy to Hostinger (DISABLED - Using Hostinger Auto-Deploy)

# This workflow is DISABLED. Hostinger handles deployment automatically
# via its built-in GitHub Deployments feature.
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type DEPLOY to confirm manual deploy'
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🚚 Checkout code
      uses: actions/checkout@v4
    
    - name: 📦 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: 📥 Install dependencies
      run: npm ci
    
    - name: 🔨 Build project
      run: npm run build
    
    - name: 📤 Deploy to Hostinger via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 89.116.133.225
        username: u136712005.attendance.legalsuccessindia.com
        password: Khurshid@Legal786
        local-dir: ./dist/
        server-dir: /public_html/
        exclude: |
          **/.git*
          **/.git*/**

